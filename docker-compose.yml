services:
  api:
    build: .
    ports: ["8000:8000"]
    environment: ["DATABASE_URL=postgresql+psycopg://postgres:postgres@db:5432/safety"]
    depends_on: [db]
  db:
    image: postgres:16-alpine
    environment: {POSTGRES_DB: safety,POSTGRES_USER: postgres,POSTGRES_PASSWORD: postgres}
