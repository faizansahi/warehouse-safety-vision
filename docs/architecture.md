# Architecture
OpenCV supplies frames, the YOLO adapter supplies typed detections, pure geometry rules create safety events, and SQLAlchemy persists history. Rules can be tested without model weights.
